services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: clawkaka
      POSTGRES_PASSWORD: clawkaka
      POSTGRES_DB: clawkaka
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
